# High-Performance-CPP-HTTP-Server

## BUILD COMMANDS

```bash
cmake -S . -B build
cmake --build build

./build/http_server
```

## Formatting

```bash
find . \( -name "*.cpp" -o -name "*.hpp" \) -exec clang-format -i {} +
```
